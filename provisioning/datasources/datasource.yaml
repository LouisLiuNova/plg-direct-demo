apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100  # 这里填 docker-compose 中的服务名
    jsonData:
      maxLines: 1000
    isDefault: true        # 设为默认数据源
